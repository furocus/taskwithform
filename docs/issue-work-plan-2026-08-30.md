# 8月30日完成に向けたIssue作成参考資料

## 完成目標

2026年8月30日までに、Classroomの課題に含まれるGoogle Formについて、Gmailの回答控えから回答確認結果を取得し、一覧画面で確認できる状態にする。

メール本文やGoogleのトークンはDBへ保存しない。「メールが見つからない」と「未回答」は同じ意味にしない。

## 7月31日〜8月2日：仕様決定

### DB担当

- 保存する回答確認結果を決める
- 課題とFormを結び付ける方法を決める
- メール本文など、保存しない情報を明確にする

### バックエンド担当

- ClassroomからForm IDを取得する流れを調査する
- Gmailの回答控えメールでForm IDを確認できるか検証する
- 必要なGoogle OAuth権限を整理する

### UI担当

- 「回答済み」「確認なし」「判定できない」の表示を決める
- 課題一覧と詳細画面のどこに状態を表示するか決める
- ローディング・エラー時の表示を決める

## 8月3日〜8月9日：基盤実装

### DB担当

- 回答確認結果を保存するテーブルを追加する
- 保存・取得・更新・削除を行うRepositoryを作る
- DB更新後も既存データが消えないことをテストする

### バックエンド担当

- Classroomから課題とForm IDを取得する処理を作る
- Gmail連携用のOAuth権限を追加する
- Gmail APIへ接続できるところまで実装する

### UI担当

- 回答状態を表示する部品を作る
- ダミーデータで各状態を確認できるようにする
- PC・スマートフォンの表示崩れを確認する

## 8月10日〜8月16日：回答判定の実装

### DB担当

- バックエンドの判定結果をDBへ保存する処理をつなぐ
- 同じFormの結果が重複しないことを確認する
- Classroomを再取得しても回答確認結果が残るようにする

### バックエンド担当

- Google Formsの回答控えだけを検索する処理を作る
- メールから対象のForm IDと回答日時を確認する
- 「回答済み」「確認なし」「判定できない」を返す
- メール本文を保存・ログ出力しないことをテストする

### UI担当

- APIと接続して実際の回答状態を表示する
- 回答確認を実行するボタンと処理中表示を追加する
- 権限不足や検索失敗をユーザー向けに表示する

## 8月17日〜8月23日：結合と修正

### DB担当

- 複数のFormが付いた課題をテストする
- データ削除時に回答確認結果も削除する
- DB操作の異常系テストを追加する

### バックエンド担当

- 複数ページに分かれたGmail検索へ対応する
- メール形式の違いによる誤判定を確認する
- トークン期限切れやAPIエラーへ対応する

### UI担当

- 複数Formの一部だけが回答済みの場合を表示する
- 回答確認日時を詳細画面へ表示する
- 空表示、再試行、エラー表示を整える

## 8月24日〜8月30日：完成確認

### DB担当

- 実データに近い条件で保存・更新・削除を確認する
- DBマイグレーションと全テストを確認する
- DB仕様をドキュメントへ残す

### バックエンド担当

- 実際の学校アカウントで動作確認する
- Gmail検索の対象が必要なメールだけになっているか確認する
- セキュリティ、ログ、エラー処理を最終確認する

### UI担当

- PCとスマートフォンで一連の操作を確認する
- 状態の説明が学生に伝わるか確認する
- 表示崩れと操作上の問題を修正する

## 完了条件

- Classroomの課題からGoogle Formを特定できる
- Gmailの回答控えから回答確認結果を取得できる
- 回答確認結果がIndexedDBへ保存される
- 課題一覧または詳細画面で回答状態を確認できる
- メール本文、回答内容、GoogleのトークンをDBへ保存しない
- PCとスマートフォンで主要な操作ができる
- テスト、型チェック、フォーマット確認、ビルドが成功する
